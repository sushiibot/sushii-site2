<!doctype html>
<html>

<head>
  <title>sushii 2 - {{ title }}</title>

  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <meta name="description" content="Chat bot for Discord">

  <!-- styles -->
  <link rel="stylesheet" href="{{assets.css.vendor}}" />
  <link rel="stylesheet" href="{{assets.css.fonts}}" />
  <link rel="stylesheet" href="{{assets.css.app}}" />
</head>

<body class="w-100">
  {{> components/nav}}
  <main class="w-100 pv5">
    {{~> page}}
  </main>
  {{> components/footer}}
</body>

</html>