{{#*inline "page"}}

<section class="ph3 ph5-ns pt6 pb4 flex-grow">
    <h1>Commands</h1>

    {{#each commands.groups }}
    <h2>{{ this.name }}</h2>
    <p>{{ this.description }}</p>

    <ul class="list pl2">
        {{#each this.commands }}
        <li class="mv3">
            <p>
                <span class="blue">{{ this.name }}</span>
                <span class="light-red">{{ this.usage }}</span>
            </p>

            <div class="bl bw1 b--gray">
                <p class="white-90 ml2">
                    {{ this.description }}
                </p>

                {{# if this.examples}}
                <p class="white-90 ml2">Examples:</p>
                {{/if}}

                {{#each this.examples}}
                <ul class="list pl2">
                    <li class="mv2">
                        {{ this }}
                    </li>
                </ul>
                {{/each}}
            </div>
        </li>
        {{/each}}
    </ul>
    {{/each}}
</section>

{{/inline}}
{{~> (parent)~}}