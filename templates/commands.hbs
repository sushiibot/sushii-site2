{{#*inline "page"}}

<section class="ph3 ph5-ns pt4">
  <h1>Commands!</h1>
  {{#each commands }}
  <h2>{{ @key }}</h2>
  <ul>
    {{#each this }}
    <li>{{ this.name }} - {{ this.description }}</li>
    {{/each}}
  </ul>
  {{/each}}
</section>

{{/inline}}
{{~> (parent)~}}